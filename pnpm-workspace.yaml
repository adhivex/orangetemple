# Dependencies allowed to run install scripts. Keep this list minimal and justified.
allowBuilds:
  # Native resolver used by eslint-import-resolver-typescript (via eslint-config-next).
  unrs-resolver: true
